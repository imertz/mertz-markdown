/**
 * Where this app comes from.
 *
 * Three surfaces offer it — the brand mark, the shortcut sheet's footer and the
 * phone actions sheet — and a URL written out three times is a URL that ends up
 * pointing at two different places.
 */
export const REPO_URL = 'https://github.com/imertz/mertz-markdown'

/** The repository's own name, for the surfaces that show it as a label. */
export const REPO_NAME = 'mertz-markdown'
